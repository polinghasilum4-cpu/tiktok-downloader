{
  "name": "multi-tools-downloader",
  "version": "1.0.0",
  "description": "Multi-tool downloader dengan YouTube, SoundCloud, dan game",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": "20.x"
  }
}
